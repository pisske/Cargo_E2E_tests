{
  "devDependencies": {
    "@shelex/cypress-allure-plugin": "^2.41.2",
    "@testing-library/cypress": "^10.1.0",
    "allure-commandline": "^2.34.1",
    "cypress": "^15.4.0",
    "cypress-plugin-xhr-toggle": "^1.2.1",
    "cypress-real-events": "^1.15.0",
    "cypress-xpath": "^2.0.1",
    "dotenv": "^17.2.2",
    "rimraf": "^6.1.0"
  },
  "scripts": {
    "clean:allure": "npx rimraf allure-results allure-report",
    "cypress:run:headless": "cypress run --headless",
    "allure:report": "npx allure-commandline generate allure-results --clean -o allure-report",
    "allure:open": "npx allure-commandline open allure-report",
    "cypress:run:allure": "npm run clean:allure && npx cypress run --headless --env allure=true && npm run allure:report && npm run allure:open",
    "cypress:run:allure:ci": "npm run clean:allure && npx cypress run --headless --env allure=true && npm run allure:report",
    "cypress:run:bookingflow": "npm run clean:allure && npx cypress run --headless --env allure=true --spec \"./cypress/e2e/Bookings/eBookingflow/EBookingFlow.cy.js\" && npm run allure:report && npm run allure:open"
  }
}
